# Data folder

Local development corpus lives here.

- `downloads/` — raw SEC EDGAR files, grouped by fiscal year
- Payloads stay out of git (they get big fast)
- Grab a starter set: `uv run data/download.py` from the repo root
